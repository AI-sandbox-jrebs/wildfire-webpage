#!/usr/bin/env bash
# Refresh data after pulling so a local preview shows current fires.
set -euo pipefail
[[ "${SKIP_DATA_REFRESH:-0}" == "1" ]] && exit 0
"$(git rev-parse --show-toplevel)/scripts/rebuild.sh"

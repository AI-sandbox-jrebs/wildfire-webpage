#!/usr/bin/env bash
# Refresh data before pushing so the published page ships a current snapshot.
# Enable with: git config core.hooksPath .githooks
set -euo pipefail
[[ "${SKIP_DATA_REFRESH:-0}" == "1" ]] && exit 0
COMMIT_DATA=1 "$(git rev-parse --show-toplevel)/scripts/rebuild.sh"
